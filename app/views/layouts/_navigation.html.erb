<header class="navbar navbar-fixed-top navbar-default">
    <div class="container">
        <div class="row">
        <%= link_to "eventsapp", root_path, class:"btn btn-lg btn-warning", id:"logo"%>
        <% if logged_in? %>
          <div class="nav_item">
            <%= link_to "Nuevo Evento", new_event_path, class:"btn btn-success pull-right"%>
          </div>
          <div class="nav_item">
            <%= link_to "Ver Perfil", user_path(current_user), class:"btn btn-success"%>
          </div>
        <% end %>
        
        <div class="nav_item">
          <%= link_to "Todos los Eventos", events_path, class:"btn btn-success"%>
        </div>
        
        <% if logged_in? %>
          <div class="nav_item">
            <%= link_to "Cerrar Sesión", logout_path, class:"btn btn-danger pull-right"%>
          </div>
        <% end %>
      </div>
    </div>
</header>